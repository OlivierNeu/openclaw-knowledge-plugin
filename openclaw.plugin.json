{
  "id": "openclaw-knowledge",
  "name": "pgvector Knowledge Base",
  "description": "Query PostgreSQL pgvector collections and inject relevant documents into agent context before each turn",
  "version": "2.0.0",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "geminiApiKey": {
        "type": "string",
        "description": "Gemini API key for embedding queries"
      },
      "postgresUrl": {
        "type": "string",
        "description": "PostgreSQL connection string for the knowledge database"
      },
      "collections": {
        "type": "array",
        "items": { "type": "string" },
        "description": "Collection names to search in the knowledge_vectors table"
      },
      "topK": {
        "type": "number",
        "description": "Max results per collection (default: 5)"
      },
      "scoreThreshold": {
        "type": "number",
        "description": "Min similarity score 0-1 (default: 0.3)"
      },
      "maxInjectChars": {
        "type": "number",
        "description": "Max characters to inject into prompt (default: 4000)"
      },
      "enabled": {
        "type": "boolean",
        "description": "Enable/disable knowledge injection (default: true)"
      }
    },
    "required": ["geminiApiKey", "postgresUrl", "collections"]
  },
  "uiHints": {
    "geminiApiKey": {
      "label": "Gemini API Key",
      "sensitive": true,
      "help": "Use ${GEMINI_API_KEY} for env var substitution"
    },
    "postgresUrl": {
      "label": "PostgreSQL URL",
      "sensitive": true,
      "help": "e.g. postgresql://openclaw:${POSTGRES_PASSWORD}@postgresql:5432/knowledge"
    },
    "collections": {
      "label": "Collections to search",
      "help": "Collection names in the knowledge_vectors table (e.g. knowledge_olivier)"
    },
    "enabled": {
      "label": "Auto-inject knowledge",
      "help": "Automatically search and inject relevant documents before each agent turn"
    }
  }
}
